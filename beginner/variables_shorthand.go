package main

import (
    "fmt"
    )

func main(){
    
    // variable shorthand declaration and value assignment
   name := "KP"
   age := 49
   height, weight := 5.7, 120
    
    
    // checking default values when explicitly no values are assigned
    fmt.Println(name)
    fmt.Println(age)
    fmt.Println(height)
    fmt.Println(weight)
    
}
